{
  "entities": {
    "Config": {
      "title": "Version Configuration",
      "description": "Global configuration specifying force update minimum version and Google Play Store link",
      "type": "object",
      "properties": {
        "minVersion": { "type": "string", "description": "Minimum required version for the client" },
        "currentVersion": { "type": "string", "description": "Latest published version of the app" },
        "playStoreUrl": { "type": "string", "description": "Direct link to Play Store list page" },
        "updateMessage": { "type": "string", "description": "Customizable update text displayed on force update dialog" },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["minVersion", "currentVersion", "playStoreUrl", "updateMessage"]
    }
  },
  "firestore": {
    "configs/global": {
      "schema": "Config",
      "description": "Single document holding active configuration for app client force update logic"
    }
  }
}
